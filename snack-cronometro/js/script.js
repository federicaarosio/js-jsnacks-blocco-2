// Cliccando su un pulsante, viene avviato un cronometro. Per fermare il cronometro, bisogna cliccare su un secondo pulsante

const btnStart = document.querySelector("button#start");
const btnStop = document.querySelector("button#stop");

btnStart.addEventListener("click",  )
//nello start

btnStop.addEventListener("cick", )
//nello stop devo mettere clearInterval